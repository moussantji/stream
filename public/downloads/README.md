# App downloads

The "App" button in the site header links to `/downloads/moviebox.apk`.

Build the Android APK from the `mobile/` Expo project (see `mobile/README.md`),
then place the resulting file here as **`moviebox.apk`**:

```
public/downloads/moviebox.apk
```

Until that file exists, the button will return a 404.
